node_modules/
dist/
*.db
.env

# Local dev overrides (publishes mesh port to host, etc.)
docker-compose.override.yml

# CLI binaries
cli/mesh-cli
cli/mesh-cli-*

# Design docs (kept in buddy/ernie)
docs/
build_onepager.py
